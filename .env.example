# metadatahub.eu environment configuration
# Copy to .env and adjust as needed.

# Domain (used by Caddy for automatic HTTPS)
DOMAIN=metadatahub.eu

# Oxigraph SPARQL endpoint (internal)
OXIGRAPH_URL=http://oxigraph:7878

# Admin email (used for Let's Encrypt)
ADMIN_EMAIL=admin@metadatahub.eu
